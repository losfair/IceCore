language: rust
script:
  - cargo build --release --verbose
  - cargo test --release --verbose
  - strip target/release/libice_core.so
matrix:
  include:
    - os: linux
      dist: trusty
deploy:
  provider: releases
  api_key: "$GITHUB_API_KEY"
  file: "target/release/libice_core.so"
  skip_cleanup: true
  on:
    tags: true
